[
  {
    "name": "multi_replace_file_content",
    "args": {
      "Description": "Add Referral and MLA endpoints in server/index.js",
      "Instruction": "Add API endpoints at the bottom of server/index.js",
      "ReplacementChunks": [
        {
          "AllowMultiple": false,
          "EndLine": 754,
          "ReplacementContent": "app.post('/api/chatbot', async (req, res) => {\n    const { query: userQuery, role, center_id, language } = req.body;\n    try {\n        const ai = new GoogleGenAI({ vertexai: { project: projectId, location: location }, project: projectId, location: location });\n        const prompt = `\n          You are the ArogyaNet District Intelligence Core, an AI assistant for a local health administration system.\n          The user is a ${role} from ${center_id || 'the district admin office'}.\n          \n          User Query: \"${userQuery}\"\n          \n          Provide a helpful, professional, and concise response. If they ask about specific stock or alerts, instruct them to visit the respective modules (e.g. 'District Logistics' or 'Command Center').\n          Respond strictly in the language requested by the user: ${language === 'hi' ? 'Hindi' : 'English'}. If the input is in English, provide an English response. If the input is in Hindi, provide a Hindi response.\n        `;\n        const response = await ai.models.generateContent({ model: 'gemini-2.5-flash', contents: prompt });\n        res.json({ reply: response.text || 'I am sorry, I could not process that request.' });\n    } catch (err) {\n        console.error(err);\n        res.status(500).json({ error: 'Chatbot error' });\n    }\n});\n\n// --- MLA DASHBOARD ENHANCEMENTS ---\napp.get('/api/mla/trend-analytics', async (req, res) => {\n    const { lang } = req.query;\n    try {\n        const ai = new GoogleGenAI({ vertexai: { project: projectId, location: location }, project: projectId, location: location });\n        const logsRes = await query('SELECT * FROM daily_logs');\n        \n        const prompt = `\n          Analyze the following district daily health logs and provide a 2-3 sentence trend summary (e.g. 'Dengue cases down by 15%').\n          Respond strictly in the language requested: ${lang === 'hi' ? 'Hindi' : 'English'}.\n          Data: ${JSON.stringify(logsRes.rows)}\n        `;\n        const response = await ai.models.generateContent({ model: 'gemini-2.5-flash', contents: prompt });\n        res.json({ trend_summary: response.text?.trim() });\n    } catch (err) {\n        console.error(err);\n        res.status(500).json({ error: 'Trend analytics error' });\n    }\n});\n\napp.get('/api/mla/governance-recommendations', async (req, res) => {\n    const { lang } = req.query;\n    try {\n        const ai = new GoogleGenAI({ vertexai: { project: projectId, location: location }, project: projectId, location: location });\n        const facRes = await query('SELECT * FROM facilities');\n        \n        const prompt = `\n          Act as an AI Governance Advisor. Review the following district facilities data.\n          Provide 3 bullet points with long-term policy suggestions or resource allocation recommendations (e.g., 'Need for new CHC in Zone B').\n          Respond strictly in the language requested: ${lang === 'hi' ? 'Hindi' : 'English'}.\n          Data: ${JSON.stringify(facRes.rows)}\n        `;\n        const response = await ai.models.generateContent({ model: 'gemini-2.5-flash', contents: prompt });\n        \n        const text = response.text || '';\n        const recs = text.split('\\n').filter(line => line.trim().startsWith('-') || line.trim().startsWith('*')).map(line => line.replace(/^[-*]\\s*/, '').trim());\n        \n        res.json({ recommendations: recs.length > 0 ? recs : [text] });\n    } catch (err) {\n        console.error(err);\n        res.status(500).json({ error: 'Governance recommendations error' });\n    }\n});\n\n// --- REFERRAL MANAGEMENT ---\napp.get('/api/referrals/:center_id', async (req, res) => {\n    const { center_id } = req.params;\n    try {\n        const result = await query('SELECT * FROM referrals');\n        // If CHC, show pending to them. If PHC, show sent by them.\n        const filtered = result.rows.filter(r => r.chc_id === center_id || r.phc_id === center_id);\n        res.json(filtered);\n    } catch (err) {\n        res.status(500).json({ error: 'Failed to fetch referrals' });\n    }\n});\n\napp.post('/api/referrals', async (req, res) => {\n    const { phc_id, chc_id, patient_name, condition_desc } = req.body;\n    try {\n        const result = await query(\n            'INSERT INTO referrals (phc_id, chc_id, patient_name, condition_desc) VALUES ($1, $2, $3, $4) RETURNING *',\n            [phc_id, chc_id, patient_name, condition_desc]\n        );\n        res.json(result.rows[0]);\n    } catch (err) {\n        res.status(500).json({ error: 'Failed to create referral' });\n    }\n});\n\napp.post('/api/referrals/accept', async (req, res) => {\n    const { id, phc_id, chc_id } = req.body;\n    try {\n        // 1. Update referral status\n        await query('UPDATE referrals SET status = $1 WHERE id = $2', ['Accepted', id]);\n        \n        // 2. AI Bed Distribution Update\n        const logRes = await query('SELECT * FROM all_logistics');\n        let allLogistics = logRes.rows[0];\n        \n        const phcBeds = allLogistics[phc_id]?.beds[0] || { occupied_beds: 0, total_beds: 0 };\n        const chcBeds = allLogistics[chc_id]?.beds[0] || { occupied_beds: 0, total_beds: 0 };\n        \n        const ai = new GoogleGenAI({ vertexai: { project: projectId, location: location }, project: projectId, location: location });\n        const prompt = `\n          A patient has been referred from ${phc_id} to ${chc_id}.\n          The current bed occupancy is:\n          PHC: ${JSON.stringify(phcBeds)}\n          CHC: ${JSON.stringify(chcBeds)}\n          \n          Provide the updated occupied_beds count for both. Usually, this means PHC occupied_beds decreases by 1 (if > 0) and CHC occupied_beds increases by 1.\n          Return ONLY a valid JSON object in this exact format:\n          {\n            \"phc_occupied\": <number>,\n            \"chc_occupied\": <number>\n          }\n        `;\n        \n        const response = await ai.models.generateContent({ model: 'gemini-2.5-flash', contents: prompt });\n        const text = (response.text || '').replace(/```json/g, '').replace(/```/g, '').trim();\n        const updatedCounts = JSON.parse(text);\n        \n        // Apply AI logic back to DB\n        if (allLogistics[phc_id] && allLogistics[phc_id].beds[0]) {\n            allLogistics[phc_id].beds[0].occupied_beds = updatedCounts.phc_occupied;\n            await query('UPDATE logistics SET itemPayload = $1 WHERE center_id = $2 AND category = $3 AND id = $4', \n               [phc_id, 'beds', allLogistics[phc_id].beds[0].id, JSON.stringify(allLogistics[phc_id].beds[0])]\n            );\n        }\n        \n        if (allLogistics[chc_id] && allLogistics[chc_id].beds[0]) {\n            allLogistics[chc_id].beds[0].occupied_beds = updatedCounts.chc_occupied;\n            await query('UPDATE logistics SET itemPayload = $1 WHERE center_id = $2 AND category = $3 AND id = $4', \n               [chc_id, 'beds', allLogistics[chc_id].beds[0].id, JSON.stringify(allLogistics[chc_id].beds[0])]\n            );\n        }\n        \n        res.json({ success: true, updatedCounts });\n    } catch (err) {\n        console.error(err);\n        res.status(500).json({ error: 'Failed to accept referral' });\n    }\n});\n\nconst PORT = process.env.PORT || 5000;\napp.listen(PORT, () => console.log(`Server running on port ${PORT}`));",
          "StartLine": 731,
          "TargetContent": "app.post('/api/chatbot', async (req, res) => {\n    const { query: userQuery, role, center_id, language } = req.body;\n    try {\n        const ai = new GoogleGenAI({ vertexai: { project: projectId, location: location }, project: projectId, location: location });\n        const prompt = `\n          You are the ArogyaNet District Intelligence Core, an AI assistant for a local health administration system.\n          The user is a ${role} from ${center_id || 'the district admin office'}.\n          \n          User Query: \"${userQuery}\"\n          \n          Provide a helpful, professional, and concise response. If they ask about specific stock or alerts, instruct them to visit the respective modules (e.g. 'District Logistics' or 'Command Center').\n          Respond strictly in the language requested by the user: ${language === 'hi' ? 'Hindi' : 'English'}. If the input is in English, provide an English response. If the input is in Hindi, provide a Hindi response.\n        `;\n        const response = await ai.models.generateContent({ model: 'gemini-2.5-flash', contents: prompt });\n        res.json({ reply: response.text || 'I am sorry, I could not process that request.' });\n    } catch (err) {\n        console.error(err);\n        res.status(500).json({ error: 'Chatbot error' });\n    }\n});\n\nconst PORT = process.env.PORT || 5000;\napp.listen(PORT, () => console.log(`Server running on port ${PORT}`));"
        }
      ],
      "TargetFile": "c:\\Users\\nithya\\OneDrive\\Desktop\\Parliament_Hack2Skill\\server\\index.js",
      "toolAction": "Add APIs for MLA features and Referrals",
      "toolSummary": "Update Server Endpoints"
    }
  }
]